<%
document[:title] = "Edit Reservation ##{@allocation.id}"
content_for :head do
%>
  <script type="text/javascript">
    $( function () {
      new Ts.FormViews.AllocationForm({
        el: $('#reservation_form')[0],
        valid_roles: ['applicant', 'reservee', 'next_of_kin'],
        multibutton: {
          items: [{name: 'approved', value: 'Submit'}]
        },
        type: 'reservation'
      })
    })
  </script>
<% end %>
<%= render :form %>